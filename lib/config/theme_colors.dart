import 'package:flutter/material.dart';

///主题颜色相关配置
class ThemeColors {
  static final Color themeColor = Color(0xFF3D7EFF);
  static final int themeColorInt = 0xFF3D7EFF;

  ///一级信息，标题，主内容文字等
  static final Color titleColor = Color(0xFF303133);

  ///普通级别文字，正文内容文字等
  static final Color normalColor = Color(0xFF606266);

  ///辅助文字，次要信息等
  static final Color auxiliaryColor = Color(0xFF909399);

  ///小段描述文字，次要文字，输入框提示文字
  static final Color subordinateColor = Color(0xFFBFC2CC);

  ///分割线，按钮边框，置灰按钮
  static final Color dividerColor = Color(0xFFEDEFF2);

  ///页面最底层背景等
  static final Color bgColor = Color(0xFFF5F7FA);
}
